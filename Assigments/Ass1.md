# Assignment One



---

## 📌 Instructions

### 1️⃣ Create a File
Create a file named:

index.html

yaml
Copy code

---

### 2️⃣ Basic Document Structure
Write the basic document structure using the following tags:

- `<!DOCTYPE html>`
- `<html>`
- `<head>`
- `<title>`
- `<body>`

---

### 3️⃣ Page Title
Set the page title to:

My First Markup Language Page

yaml
Copy code

---

### 4️⃣ Body Content
Inside the `<body>` tag, add the following:

- `<h1>` → Write **your name**
- `<h2>` → Write:  
  **Markup Language Practice**
- `<p>` → Write a short paragraph explaining **what a Markup Language is**
  - Use `<strong>` or `<em>` **at least once**

---

### 5️⃣ Create a List
Create an unordered list using `<ul>` and add **3 things you like**, for example:

- Coding  
- Reading  
- Football  

---

### 6️⃣ Add an Image
Add an image using the `<img>` tag.

⚠️ The `alt` attribute is **required**.

---

### 7️⃣ Add a Link
Create a link that points to:

https://google.com

yaml
Copy code

Requirements:
- Use the `<a>` tag
- Add `target="_blank"` to open the link in a new tab

---

### 8️⃣ Create a Simple Form
Create a simple form that includes:

- **Name** → text input
- **Message** → textarea
- **Send** → submit button

---

## ⭐ Bonus (Optional)
- Add a horizontal line using `<hr>`
- Add an ordered list `<ol>` with steps or instructions

---

## ✅ Checklist
✔ Correct document structure  
✔ Proper use of markup tags  
✔ Page opens without errors  

---

✨ **Good luck and happy coding!**